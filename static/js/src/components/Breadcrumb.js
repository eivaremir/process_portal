
function Breadcrumb({ sectionlist }) {
    
    return (
        <>
        {/*<div  className="breadcrumb-container" style={{height: "40px", display:"flex"}}>
                {
                    sectionlist.map((section)=>(
                        <a href="#!" className="breadcumb-item">{section}</a>
                    ))
                }
               
            
            
            </div>*/}
        </>
        
    )
}

export default Breadcrumb
